@model SmartPlanner.Models.Event
@using SmartPlanner.Models.Enums

@{
    ViewData["Title"] = "Редактирование мероприятия";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 shadow-sm p-4 bg-white rounded">
            <h2 class="mb-4">✏️ Редактирование мероприятия</h2>
            
            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="CreatedAt" />
                
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="mb-3">
                    <label asp-for="Title" class="form-label fw-bold">Название</label>
                    <input asp-for="Title" class="form-control" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="StartTime" class="form-label">Время начала</label>
                        <input asp-for="StartTime" type="datetime-local" class="form-control" />
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="EndTime" class="form-label">Время окончания</label>
                        <input asp-for="EndTime" type="datetime-local" class="form-control" />
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="LifeAreaId" class="form-label">Сфера жизни</label>
                        <select asp-for="LifeAreaId" class="form-select" asp-items="ViewBag.LifeAreas">
                            <option value="">-- Не выбрано --</option>
                        </select>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="LocationId" class="form-label">Локация</label>
                        <select asp-for="LocationId" class="form-select" asp-items="ViewBag.Locations">
                            <option value="">-- Не выбрано --</option>
                        </select>
                    </div>
                </div>

                <div class="card bg-light mb-3 border-primary">
                    <div class="card-body">
                        <div class="form-check mb-2">
                            <input asp-for="IsRecurring" class="form-check-input" id="isRecurringCheck" />
                            <label asp-for="IsRecurring" class="form-check-label">Повторяющееся мероприятие</label>
                        </div>
                        <div id="recurrenceFields" style="display: @(Model.IsRecurring ? "block" : "none");">
                            <div class="row">
                                <div class="col-md-6">
                                    <label asp-for="RecurrenceType" class="form-label">Тип повтора</label>
                                    <select asp-for="RecurrenceType" class="form-select" asp-items="Html.GetEnumSelectList<RecurrenceType>()"></select>
                                </div>
                                <div class="col-md-6">
                                    <label asp-for="RecurrenceEndDate" class="form-label">Повторять до</label>
                                    <input asp-for="RecurrenceEndDate" type="date" class="form-control" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="mb-3">
                    <label asp-for="EnergyRequired" class="form-label text-primary">⚡ Энергия (1-5)</label>
                    <input asp-for="EnergyRequired" type="number" class="form-control" min="1" max="5" />
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-secondary">Отмена</a>
                    <button type="submit" class="btn btn-primary px-5">Сохранить изменения</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('isRecurringCheck').addEventListener('change', function() {
            document.getElementById('recurrenceFields').style.display = this.checked ? 'block' : 'none';
        });
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}